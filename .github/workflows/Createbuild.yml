name: Java CI/CD with Maven

# ==============================================================================
# TRIGGERS
# - develop => DEV (port 9091)
# - main    => PROD (port 9092)
# ==============================================================================
on:
  push:
    branches: [ "develop", "main" ]
  workflow_dispatch:

# ==============================================================================
# VARIABLES GLOBALES
# ==============================================================================
env:
  JAVA_VERSION: '17'
  JAVA_DISTRIBUTION: 'temurin'
  MAVEN_OPTS: '-Xmx1024m'

jobs:

  # ============================================================================
  # JOB 1 : BUILD & TEST
  # Compilation du code et exécution des tests
  # ============================================================================
  build:
    name: 🔨 Build & Test Application
    runs-on: ubuntu-latest

    outputs:
      artifact-name: ${{ steps.artifact.outputs.name }}
      build-version: ${{ steps.version.outputs.version }}
      tests-passed: ${{ steps.tests.outputs.passed }}

    steps:
      # --------------------------------------------------------------------------
      # Récupération du code source
      # --------------------------------------------------------------------------
      - name: 📥 Checkout source code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # --------------------------------------------------------------------------
      # Configuration Java
      # --------------------------------------------------------------------------
      - name: ☕ Set up JDK ${{ env.JAVA_VERSION }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: ${{ env.JAVA_DISTRIBUTION }}
          cache: maven

      # --------------------------------------------------------------------------
      # Extraction de la version depuis pom.xml
      # --------------------------------------------------------------------------
      - name: 🏷️ Extract version from pom.xml
        id: version
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "📦 Version détectée: $VERSION"

      # --------------------------------------------------------------------------
      # ✅ AMÉLIORATION 1 : Exécution des tests automatisés
      # --------------------------------------------------------------------------
      - name: 🧪 Run unit tests
        id: tests
        run: |
          echo "🧪 Exécution des tests unitaires..."
          
          # Exécution des tests avec rapport détaillé
          mvn test -B
          
          # Vérification du résultat
          if [ $? -eq 0 ]; then
            echo "✅ Tous les tests sont passés"
            echo "passed=true" >> $GITHUB_OUTPUT
          else
            echo "❌ Des tests ont échoué"
            echo "passed=false" >> $GITHUB_OUTPUT
            exit 1
          fi

      # --------------------------------------------------------------------------
      # Affichage du rapport de tests
      # --------------------------------------------------------------------------
      - name: 📊 Display test summary
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 RAPPORT DE TESTS"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          
          if [ -f target/surefire-reports/*.xml ]; then
            # Comptage des tests
            TOTAL=$(grep -r "tests=" target/surefire-reports/*.xml | head -1 | sed 's/.*tests="\([0-9]*\)".*/\1/')
            FAILURES=$(grep -r "failures=" target/surefire-reports/*.xml | head -1 | sed 's/.*failures="\([0-9]*\)".*/\1/')
            ERRORS=$(grep -r "errors=" target/surefire-reports/*.xml | head -1 | sed 's/.*errors="\([0-9]*\)".*/\1/')
            SUCCESS=$((TOTAL - FAILURES - ERRORS))
          
            echo "✅ Tests réussis  : $SUCCESS"
            echo "❌ Tests échoués  : $FAILURES"
            echo "⚠️  Erreurs       : $ERRORS"
            echo "📝 Total         : $TOTAL"
          else
            echo "ℹ️  Aucun rapport de test disponible"
          fi
          
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # --------------------------------------------------------------------------
      # Upload des rapports de tests
      # --------------------------------------------------------------------------
      - name: 📤 Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: target/surefire-reports/
          retention-days: 30

      # --------------------------------------------------------------------------
      # Compilation et packaging (après tests réussis)
      # --------------------------------------------------------------------------
      - name: 🔧 Build application with Maven
        run: |
          echo "🚀 Démarrage de la compilation..."
          mvn clean package -DskipTests
          echo "✅ Compilation terminée avec succès"

      # --------------------------------------------------------------------------
      # Vérification de l'artefact produit
      # --------------------------------------------------------------------------
      - name: 🔍 Verify build artifact
        run: |
          if [ ! -f target/*.jar ]; then
            echo "❌ Erreur: Aucun fichier JAR trouvé dans target/"
            exit 1
          fi
          
          JAR_SIZE=$(du -h target/*.jar | cut -f1)
          echo "✅ JAR trouvé (taille: $JAR_SIZE)"
          ls -lh target/*.jar

      # --------------------------------------------------------------------------
      # Normalisation du nom du JAR
      # --------------------------------------------------------------------------
      - name: 📝 Rename JAR to app.jar
        id: artifact
        run: |
          mv target/*.jar target/app.jar
          echo "name=app.jar" >> $GITHUB_OUTPUT
          echo "✅ Fichier renommé: app.jar"

      # --------------------------------------------------------------------------
      # Upload de l'artefact pour les jobs suivants
      # --------------------------------------------------------------------------
      - name: 📤 Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: spring-boot-app
          path: target/app.jar
          retention-days: 5

  # ============================================================================
  # JOB 2 : DETERMINATION DE L'ENVIRONNEMENT
  # Calcul des paramètres selon la branche
  # ============================================================================
  configure:
    name: ⚙️ Configure Environment
    runs-on: ubuntu-latest
    needs: build

    outputs:
      app-dir: ${{ steps.config.outputs.app-dir }}
      port: ${{ steps.config.outputs.port }}
      log-file: ${{ steps.config.outputs.log-file }}
      env-name: ${{ steps.config.outputs.env-name }}
      branch: ${{ steps.config.outputs.branch }}
      service-name: ${{ steps.config.outputs.service-name }}

    steps:
      # --------------------------------------------------------------------------
      # Détermination des paramètres d'environnement
      # --------------------------------------------------------------------------
      - name: 🎯 Set environment configuration
        id: config
        run: |
          BRANCH_NAME="${{ github.ref_name }}"
          
          if [ "${{ github.ref }}" = "refs/heads/develop" ]; then
            ENV_NAME="DEV"
            APP_DIR="/opt/apps/dev/java/TestGithubActions"
            PORT="9091"
            LOG_FILE="app-dev.log"
            SERVICE_NAME="testgithubactions-dev"
          else
            ENV_NAME="PROD"
            APP_DIR="/opt/apps/prod/java/TestGithubActions"
            PORT="9092"
            LOG_FILE="app-prod.log"
            SERVICE_NAME="testgithubactions-prod"
          fi
          
          # Export des outputs
          echo "env-name=$ENV_NAME" >> $GITHUB_OUTPUT
          echo "app-dir=$APP_DIR" >> $GITHUB_OUTPUT
          echo "port=$PORT" >> $GITHUB_OUTPUT
          echo "log-file=$LOG_FILE" >> $GITHUB_OUTPUT
          echo "branch=$BRANCH_NAME" >> $GITHUB_OUTPUT
          echo "service-name=$SERVICE_NAME" >> $GITHUB_OUTPUT
          
          # Affichage récapitulatif
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🎯 Configuration du déploiement"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📍 Environnement : $ENV_NAME"
          echo "🌿 Branche       : $BRANCH_NAME"
          echo "📂 Répertoire    : $APP_DIR"
          echo "🔌 Port          : $PORT"
          echo "📄 Log           : $LOG_FILE"
          echo "🔧 Service       : $SERVICE_NAME"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  # ============================================================================
  # JOB 3 : DEPLOIEMENT SUR LA VM
  # Transfert et démarrage de l'application
  # ============================================================================
  deploy:
    name: 🚀 Deploy to ${{ needs.configure.outputs.env-name }}
    runs-on: ubuntu-latest
    needs: [build, configure]

    environment:
      name: ${{ needs.configure.outputs.env-name }}
      url: http://207.180.240.150:${{ needs.configure.outputs.port }}

    steps:
      - name: 📥 Download build artifact
        uses: actions/download-artifact@v4
        with:
          name: spring-boot-app
          path: ./artifact

      - name: 🔍 Verify downloaded artifact
        run: |
          echo "📦 Contenu du répertoire artifact:"
          ls -lh ./artifact/
          
          if [ ! -f ./artifact/app.jar ]; then
            echo "❌ Erreur: app.jar introuvable"
            exit 1
          fi
          
          JAR_SIZE=$(du -h ./artifact/app.jar | cut -f1)
          echo "✅ Artefact vérifié (taille: $JAR_SIZE)"

      - name: 📁 Prepare remote directory
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            APP_DIR="${{ needs.configure.outputs.app-dir }}"
            mkdir -p "$APP_DIR"
            rm -rf "$APP_DIR/app.jar" "$APP_DIR/artifact"
            echo "✅ Répertoire prêt"

      - name: 📤 Transfer JAR to remote VM
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p ${{ secrets.PORT }} ${{ secrets.HOST }} >> ~/.ssh/known_hosts 2>/dev/null
          
          echo "📤 Transfert en cours..."
          base64 ./artifact/app.jar | ssh -i ~/.ssh/deploy_key \
              -p ${{ secrets.PORT }} \
              -o StrictHostKeyChecking=no \
              "${{ secrets.USERNAME }}@${{ secrets.HOST }}" \
              "base64 -d > ${{ needs.configure.outputs.app-dir }}/app.jar"
          
          rm -f ~/.ssh/deploy_key
          echo "✅ Transfert terminé"

      - name: ✅ Verify transfer
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            APP_DIR="${{ needs.configure.outputs.app-dir }}"
            if [ ! -f "$APP_DIR/app.jar" ]; then
              echo "❌ ERREUR: JAR non transféré"
              exit 1
            fi
            echo "✅ JAR vérifié: $(du -h $APP_DIR/app.jar | cut -f1)"

      - name: 🔧 Setup systemd service
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            APP_DIR="${{ needs.configure.outputs.app-dir }}"
            PORT="${{ needs.configure.outputs.port }}"
            LOG_FILE="${{ needs.configure.outputs.log-file }}"
            ENV_NAME="${{ needs.configure.outputs.env-name }}"
            SERVICE_NAME="${{ needs.configure.outputs.service-name }}"
            
            # Détection Java
            JAVA_HOME=$(readlink -f /usr/bin/java | sed "s:/bin/java::")
            JAVA_BIN=$(which java)
            
            echo "☕ Java: $JAVA_BIN"
            java -version 2>&1 | head -1
            
            # Création service systemd
            sudo tee /etc/systemd/system/$SERVICE_NAME.service > /dev/null << EOF
            [Unit]
            Description=TestGithubActions Spring Boot ($ENV_NAME)
            After=network.target
            
            [Service]
            Type=simple
            User=$USER
            WorkingDirectory=$APP_DIR
            Environment="JAVA_HOME=$JAVA_HOME"
            Environment="PATH=$JAVA_HOME/bin:/usr/local/bin:/usr/bin:/bin"
            
            ExecStart=$JAVA_BIN -Xms256m -Xmx512m -XX:+UseG1GC -Dspring.profiles.active=$(echo $ENV_NAME | tr '[:upper:]' '[:lower:]') -Dspring.main.banner-mode=off -Djava.awt.headless=true -jar $APP_DIR/app.jar --server.port=$PORT
            
            Restart=on-failure
            RestartSec=10
            TimeoutStopSec=30
            
            StandardOutput=append:$APP_DIR/$LOG_FILE
            StandardError=append:$APP_DIR/$LOG_FILE
            
            LimitNOFILE=65536
            
            [Install]
            WantedBy=multi-user.target
            EOF
            
            sudo systemctl daemon-reload
            sudo systemctl enable $SERVICE_NAME
            echo "✅ Service configuré"

      - name: 🛑 Stop existing service
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            SERVICE_NAME="${{ needs.configure.outputs.service-name }}"
            PORT="${{ needs.configure.outputs.port }}"
            
            # Arrêt service
            sudo systemctl stop $SERVICE_NAME 2>/dev/null || true
            sleep 2
            
            # Kill processus sur le port
            if lsof -ti:$PORT >/dev/null 2>&1; then
              echo "⚠️  Nettoyage du port $PORT"
              sudo kill -9 $(lsof -ti:$PORT) 2>/dev/null || true
              sleep 2
            fi
            
            echo "✅ Prêt pour le redémarrage"

      - name: ▶️  Start new service
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            SERVICE_NAME="${{ needs.configure.outputs.service-name }}"
            APP_DIR="${{ needs.configure.outputs.app-dir }}"
            LOG_FILE="${{ needs.configure.outputs.log-file }}"
            PORT="${{ needs.configure.outputs.port }}"
            
            echo "🚀 Démarrage de $SERVICE_NAME..."
            
            # Suppression ancien log
            rm -f "$APP_DIR/$LOG_FILE"
            
            # Démarrage
            sudo systemctl start $SERVICE_NAME
            sleep 15
            
            # Vérification
            if sudo systemctl is-active --quiet $SERVICE_NAME; then
              echo "✅ Service actif"
              sudo systemctl status $SERVICE_NAME --no-pager -l | head -20
            else
              echo "❌ ERREUR: Service inactif"
              echo ""
              echo "📋 Logs systemd:"
              sudo journalctl -u $SERVICE_NAME -n 30 --no-pager
              echo ""
              echo "📋 Logs applicatifs:"
              if [ -f "$APP_DIR/$LOG_FILE" ]; then
                cat "$APP_DIR/$LOG_FILE"
              else
                echo "⚠️  Log non créé"
                echo "🔍 Test manuel:"
                cd $APP_DIR
                timeout 5 java -jar app.jar --server.port=$PORT 2>&1 || true
              fi
              exit 1
            fi

  # ============================================================================
  # JOB 4 : VERIFICATION POST-DEPLOIEMENT
  # Tests de santé et validation du déploiement
  # ============================================================================
  verify:
    name: ✅ Verify Deployment
    runs-on: ubuntu-latest
    needs: [configure, deploy]

    steps:
      # --------------------------------------------------------------------------
      # Vérification du service systemd
      # --------------------------------------------------------------------------
      - name: 🔍 Check service health
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            SERVICE_NAME="${{ needs.configure.outputs.service-name }}"
            PORT="${{ needs.configure.outputs.port }}"
            APP_DIR="${{ needs.configure.outputs.app-dir }}"
            LOG_FILE="${{ needs.configure.outputs.log-file }}"
            
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🔍 VERIFICATION DU SERVICE"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            # 1️⃣ Vérification du service systemd
            echo ""
            echo "1️⃣  Service systemd:"
            if sudo systemctl is-active --quiet $SERVICE_NAME; then
              # Récupération des infos du service
              PID=$(sudo systemctl show -p MainPID --value $SERVICE_NAME)
              UPTIME=$(ps -p $PID -o etime= | tr -d ' ')
              MEM=$(ps -p $PID -o rss= | awk '{printf "%.2f MB", $1/1024}')
            
              echo "   ✅ Service actif"
              echo "   🆔 PID: $PID"
              echo "   ⏱️  Uptime: $UPTIME"
              echo "   💾 Mémoire: $MEM"
            
              # Statut détaillé
              echo ""
              sudo systemctl status $SERVICE_NAME --no-pager -l | head -15
            else
              echo "   ❌ ERREUR: Service inactif"
              echo ""
              echo "   📋 Logs d'erreur:"
              sudo journalctl -u $SERVICE_NAME -n 30 --no-pager
              exit 1
            fi
            
            # 2️⃣ Vérification du port
            echo ""
            echo "2️⃣  Port d'écoute:"
            if lsof -i:$PORT >/dev/null 2>&1; then
              echo "   ✅ Port $PORT en écoute"
              lsof -i:$PORT | grep LISTEN | awk '{print "   🔌 " $1 " (PID: " $2 ")"}'
            else
              echo "   ❌ ERREUR: Port $PORT non accessible"
              exit 1
            fi
            
            # 3️⃣ Vérification des fichiers
            echo ""
            echo "3️⃣  Fichiers:"
            if [ -f "$APP_DIR/app.jar" ]; then
              JAR_SIZE=$(du -h "$APP_DIR/app.jar" | cut -f1)
              echo "   ✅ app.jar présent ($JAR_SIZE)"
            fi
            
            if [ -f "$APP_DIR/$LOG_FILE" ]; then
              LOG_SIZE=$(du -h "$APP_DIR/$LOG_FILE" | cut -f1)
              LOG_LINES=$(wc -l < "$APP_DIR/$LOG_FILE")
              echo "   ✅ $LOG_FILE présent ($LOG_SIZE, $LOG_LINES lignes)"
            fi
            
            # 4️⃣ Analyse des logs
            echo ""
            echo "4️⃣  Analyse des logs:"
            if [ -f "$APP_DIR/$LOG_FILE" ]; then
              if tail -100 "$APP_DIR/$LOG_FILE" | grep -q "Started.*Application in"; then
                STARTUP_TIME=$(tail -100 "$APP_DIR/$LOG_FILE" | grep "Started.*Application in" | tail -1 | grep -oP '\d+\.\d+(?= seconds)' || echo "N/A")
                echo "   ✅ Application démarrée avec succès"
                echo "   ⚡ Temps de démarrage: ${STARTUP_TIME}s"
              fi
            
              if tail -100 "$APP_DIR/$LOG_FILE" | grep -qi "error\|exception"; then
                echo "   ⚠️  Erreurs détectées dans les logs"
              else
                echo "   ✅ Aucune erreur détectée"
              fi
            fi
            
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

      # --------------------------------------------------------------------------
      # Test de connectivité HTTP
      # --------------------------------------------------------------------------
      - name: 🌐 HTTP connectivity test
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            PORT="${{ needs.configure.outputs.port }}"
            
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🌐 TEST DE CONNECTIVITE HTTP"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""
            
            MAX_ATTEMPTS=10
            ATTEMPT=1
            SUCCESS=false
            
            while [ $ATTEMPT -le $MAX_ATTEMPTS ]; do
              echo "🔄 Tentative $ATTEMPT/$MAX_ATTEMPTS..."
            
              HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:$PORT/ 2>/dev/null || echo "000")
            
              if [ "$HTTP_CODE" != "000" ]; then
                RESPONSE_TIME=$(curl -s -o /dev/null -w "%{time_total}" http://localhost:$PORT/ 2>/dev/null)
                echo "   ✅ Réponse HTTP: $HTTP_CODE"
                echo "   ⚡ Temps de réponse: ${RESPONSE_TIME}s"
                SUCCESS=true
                break
              else
                echo "   ⏳ Pas de réponse, attente de 5 secondes..."
                sleep 5
              fi
            
              ATTEMPT=$((ATTEMPT + 1))
            done
            
            echo ""
            
            if [ "$SUCCESS" = true ]; then
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo "✅ DEPLOIEMENT REUSSI"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo "🌍 URL: http://207.180.240.150:$PORT"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            else
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              echo "❌ ECHEC DU DEPLOIEMENT"
              echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
              exit 1
            fi

      # --------------------------------------------------------------------------
      # Affichage des logs en cas d'échec
      # --------------------------------------------------------------------------
      - name: 📋 Display logs on failure
        if: failure()
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.PORT }}
          script: |
            SERVICE_NAME="${{ needs.configure.outputs.service-name }}"
            
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "📋 LOGS DU SERVICE (50 dernières lignes)"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            
            sudo journalctl -u $SERVICE_NAME -n 50 --no-pager
            
            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

  # ============================================================================
  # JOB 5 : NOTIFICATION
  # Notification du statut de déploiement
  # ============================================================================
  notify:
    name: 📢 Notify Deployment Status
    runs-on: ubuntu-latest
    needs: [configure, build, verify]
    if: always()

    steps:
      - name: 📢 Deployment summary
        run: |
          ENV_NAME="${{ needs.configure.outputs.env-name }}"
          PORT="${{ needs.configure.outputs.port }}"
          BRANCH="${{ needs.configure.outputs.branch }}"
          VERSION="${{ needs.build.outputs.build-version }}"
          SERVICE_NAME="${{ needs.configure.outputs.service-name }}"
          
          if [ "${{ needs.verify.result }}" = "success" ]; then
            STATUS="✅ SUCCES"
            COLOR="🟢"
          else
            STATUS="❌ ECHEC"
            COLOR="🔴"
          fi
          
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "$COLOR DEPLOIEMENT $ENV_NAME - $STATUS"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "🌿 Branche      : $BRANCH"
          echo "📍 Environnement: $ENV_NAME"
          echo "📦 Version      : $VERSION"
          echo "🔌 Port         : $PORT"
          echo "🔧 Service      : $SERVICE_NAME"
          echo "🌍 URL          : http://207.180.240.150:$PORT"
          echo "👤 Auteur       : ${{ github.actor }}"
          echo "📝 Commit       : ${{ github.sha }}"
          echo "⏱️  Date         : $(date '+%Y-%m-%d %H:%M:%S')"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"