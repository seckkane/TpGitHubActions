# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "main", "develop" ]
  workflow_dispatch:
jobs:
  Create-Folder:
    runs-on: ubuntu-latest
    steps:
    - name: Create Folder on remote vm
      uses: appleboy/ssh-action@v1
      wils -lath:
        host: ${{ secrets.HOST }}
        username: ${{ secrets.USERNAME }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        port: ${{ secrets.PORT }}
        script: |
          set -x           # active le "mode debug", affiche chaque commande avant exécution
          echo "Je suis connecté sur la VM"
          whoami
          echo "Création du premier dossier"
          mkdir -p /root/test-github-actions
          echo "Création du deuxième dossier"
          mkdir -p /root/test-github-actions-2
          echo "Liste des dossiers dans /root"
          ls -la /root

  build:
    runs-on: ubuntu-latest
    steps:
    - name: Téléchargement de sources
      uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn -Dmaven.test.skyp=true -B package --file pom.xml
